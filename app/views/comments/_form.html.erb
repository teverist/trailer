<%= form_for @post.comments do |f| %>
  <div class="field form-group">
    <%= f.label :body %><br>
    <%= f.text_area :body, class: 'form-control' %>
  </div>
  <div class="actions form-group">
    <%= f.submit "Make comment", class: 'btn btn-primary'  %>
  </div>
<% end %>